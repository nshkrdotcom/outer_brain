# OuterBrain Token Meter

Deterministic token metering for Phase D cost and budget fabric.

The package accepts provider-family refs, model refs, tenant refs, and bounded
token counts. It never calls providers for counting and never accepts raw
prompt, completion, tool, or provider payload bodies.
