# Ground Plane Projection Bridge

Normalizes semantic runtime rows into projection publication shapes.

Phase 7 projection publication shapes carry memory-default persistence posture
and redacted publication refs. The bridge does not call durable substrates from
memory paths and cannot expose raw prompt, provider payload, token, or auth
material in projection evidence.
